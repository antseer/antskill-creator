# L2 · MCP 新建聚合接口

> 跨时间、跨市场、跨用户的**聚合型**预计算接口。业务特性足够通用,适合 MCP 维护,不适合放在 Skill L3。

## 责任
- 列出本 Skill 依赖的 L2 缺口(已由 S2 路由审计识别)
- 定义每个 L2 接口的期望形态(指向 `data-prd.md`)
- 给出降级路径(L3 / L4 是否能兜底,性能代价是什么)

## 交付物
| 文件 | 内容 |
|---|---|
| `interfaces.md` | L2 接口索引 + 降级路径 |

## 依赖
- 上游: 无(L2 聚合不依赖本 Skill 的 L1)—— 后端独立预计算
- 下游: L4-llm(`hero-narrative.md` / `hit-rate-narrative.md` 消费 DATA-03 结果)

## 状态
- L2 全部待后端补齐(1 条 · DATA-03 P1)。P1 可降级,Skill 仍可运行,说服力下降。
